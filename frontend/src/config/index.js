/**
 * Configuration exports for the frontend application.
 * Provides a single entry point for all configuration needs.
 */

export { default as appConfig, apiConfig, uiConfig, cacheConfig, performanceConfig } from './appConfig';

// Re-export for convenience
export default appConfig;